import React from 'react'

interface TableProps {
    period: string;
}

export const Table = ({ period }) => {
  return (
    <table className="table-auto border-collapse border-t border-b border-gray-300 w-[80%] mt-10">
        <thead>
            ds
        </thead>
    </table>
  )
}
